# Mainboard Netlist Reference

`Sheet1.NET` is included here as the text-level connectivity reference used to derive the current mainboard documentation.

Use this together with:

- `docs/specs/ALTIUM_MAINBOARD_COMPLETE_FINAL_FROM_SHEET1_NET_KR.md`

The Markdown document is the curated explanation.
The `.NET` file is the raw netlist reference.
