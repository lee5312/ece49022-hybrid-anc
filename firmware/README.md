# Firmware Layout

## Active Target

- `teensy41/` is the active mainboard firmware target.

## Legacy Prototype Targets

- `esp32/` contains early sensor-hub prototype code.
- `stm32/` contains early DSP/control prototype code.

These legacy folders are preserved for design history, but they are not the current deployment baseline.
